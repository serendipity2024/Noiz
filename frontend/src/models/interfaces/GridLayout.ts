/* eslint-disable import/no-default-export */
export const DEFAULT_GRID_LAYOUT: ZGridLayout = {
  x: 0,
  y: 0,
  w: 0,
  h: 0,
};

export default interface ZGridLayout {
  x: number;
  y: number;
  w: number;
  h: number;
}
